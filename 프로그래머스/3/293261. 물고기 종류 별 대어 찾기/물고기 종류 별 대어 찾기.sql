SELECT
    C.ID, B.FISH_NAME, A.LENGTH
    
FROM (
SELECT FISH_TYPE, MAX(LENGTH) AS LENGTH

FROM FISH_INFO

GROUP BY FISH_TYPE
) A

JOIN FISH_INFO C
    ON A.FISH_TYPE = C.FISH_TYPE AND A.LENGTH = C.LENGTH
JOIN FISH_NAME_INFO B
    ON A.FISH_TYPE = B.FISH_TYPE




    
    