-- 코드를 작성해주세요
WITH P AS (
    SELECT ID, GENOTYPE
    FROM ECOLI_DATA
)
SELECT 
    C.ID,
    C.GENOTYPE,
    P.GENOTYPE AS PARENT_GENOTYPE

FROM
ECOLI_DATA C
JOIN P ON C.PARENT_ID = P.ID

WHERE
    C.GENOTYPE & P.GENOTYPE = P.GENOTYPE

ORDER BY
    ID 